pipeline {
    agent {
      kubernetes  {
            label 'jenkins-slave'
        }
    }
  environment {
      IMAGE_REPO = "nkhare/rsvp-demo"
      // Instead of teamcloudyuga, use your repo name
  }
}
